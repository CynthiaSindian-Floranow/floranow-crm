# Copy to .env and fill in. The tool refuses to run against prod for now.

# Twenty CRM — dev workspace (same values as ../data-model/.env; if absent
# here, the tool falls back to reading ../data-model/.env)
TWENTY_DEV_URL="https://<dev-crm-host>"
TWENTY_DEV_API_KEY=""

# Floranow ERP — dev
ERP_DEV_URL="https://erp-dev.floranow.com"
# Value of FLORANOW_API_SHARED_KEY on the dev ERP
ERP_DEV_API_KEY=""
