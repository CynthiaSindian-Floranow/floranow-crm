// Above this many matches the dropdown asks for a narrower search rather than
// rendering a menu item per city. There are 24k cities; the browser will not
// enjoy all of them at once.
export const LOCALITY_OPTIONS_SHOWN_LIMIT = 60;
